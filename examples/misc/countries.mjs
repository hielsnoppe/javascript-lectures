/**
 * Diese Datei braucht ihr nicht verstehen!
 * 
 * ( Dürft ihr aber, wenn ich wollt. ;-) )
 */

import { createRequire } from "module";
const require = createRequire(import.meta.url);
export default require("./countries.json");